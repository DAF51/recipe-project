<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Your Recipes</h1>
    <ul>
    <% todos.forEach( el => { %>
            <li class='todoItem' data-id='<%=el._id%>'>
                <span class="recipeName"><%= el.recipeName %></span>
                <span class="catagory"><%=el.catagory %></span>
                <p class="recipe"><%=el.recipe%></p>
                <form action=<%=`/edit/${el._id}`%> method="GET"><button>Edit</button></form>
                <span class='del'> Delete </span>
            </li>
    <% }) %>    
    </ul>

    <h2><%= user.userName %> has <%= left %> things left to do.</h2>

    <form action="/create" method="GET"><button>Create recipe</button></form>

    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>