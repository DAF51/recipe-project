<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/SASSWORKSHOP/style.css">
</head>
<body>
    <section class="index">
        <% if (locals.messages.errors) { %>
            <% messages.errors.forEach( el => { %>
                <div class="alert alert-danger"><%= el.msg %></div>
            <% }) %>    
        <% } %>
        <% if (locals.messages.info) { %>
            <% messages.info.forEach( el => { %>
                <div class="alert alert-danger"><%= el.msg %></div>
            <% }) %>    
        <% } %>
        <form class="form" action="/login" method="POST">
            <input class="form__input form__input__style form__input__color" type="email" name="email" placeholder="Email">
            <input class="form__input form__input__style form__input__color" type="password" name="password" placeholder="Password">
            <input class="btn btn__style btn__color" type="submit">
        </form>
    </section>
</body>
</html>